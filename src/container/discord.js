import React from 'react';
import {Info, Table} from '../components';
import * as ROUTES from '../constants/routes';

export function DiscordContainer({ children }) {
    return (
        <Table>
            <Table.Title>Discord</Table.Title>
        </Table>
    );
}