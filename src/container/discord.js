import React from 'react';
import { Info } from '../components';
import * as ROUTES from '../constants/routes';

export function DiscordContainer({ children }) {
    return (
        <Info>
            <Info.Text>Discord</Info.Text>
        </Info>
    );
}